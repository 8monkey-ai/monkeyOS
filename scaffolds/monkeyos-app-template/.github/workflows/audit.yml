name: Repository audit
on:
  schedule:
    - cron: "17 3 * * 1"
  workflow_dispatch:
permissions:
  contents: read
  issues: write
jobs:
  audit:
    uses: monkeyos-org/monkeyos-platform/.github/workflows/audit.yml@v1
    secrets: inherit
