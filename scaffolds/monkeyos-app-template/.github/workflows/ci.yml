name: CI
on:
  pull_request:
  push:
    branches: [main]
permissions:
  contents: read
  packages: write
jobs:
  ci:
    uses: monkeyos-org/monkeyos-platform/.github/workflows/ci.yml@v1
    secrets: inherit
