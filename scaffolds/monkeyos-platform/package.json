{
  "name": "monkeyos-platform",
  "version": "2.2.0",
  "private": true,
  "type": "module",
  "scripts": {
    "format": "oxfmt .",
    "format:check": "oxfmt --check .",
    "lint": "oxlint --deny-warnings .",
    "typecheck": "bun ./node_modules/typescript/bin/tsc --noEmit",
    "test": "bun test",
    "check": "bun run format:check && bun run lint && bun run typecheck && bun test",
    "identity": "bun provisioning/identity-cli.ts",
    "provision": "bun provisioning/provision-app.ts",
    "skills:sync": "bun scripts/sync-skills.ts",
    "kamal:config": "bun deployment/kamal/generate-config.ts"
  },
  "devDependencies": {
    "@types/bun": "^1.4.0",
    "oxfmt": "^0.64.0",
    "oxlint": "^1.79.0",
    "typescript": "^7.0.2",
    "zod": "^4.4.3"
  }
}
